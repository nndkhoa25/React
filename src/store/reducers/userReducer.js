
//Gia tri khoi tao ban dau
const initialState = {
    useData: {}
}
// useData: Chua thong tin nguoi dung sau khi dang nhap thanh cong

const userReducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}

export default userReducer;